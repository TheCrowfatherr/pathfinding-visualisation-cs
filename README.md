# Visualisation of pathfinding algorithms written in C#. Since basic C# libraries do not support certain data structure (namely Priority Queue), C5 library for generic data structures has been used.

